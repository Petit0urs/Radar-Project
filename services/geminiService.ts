import { Project, Material } from "../types";

// Service désactivé car l'IA n'est plus nécessaire
export const analyzeReality = async (projects: Project[], materials: Material[]): Promise<string> => {
  return "Analyse IA désactivée.";
};